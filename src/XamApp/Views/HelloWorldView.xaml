<?xml version="1.0" encoding="utf-8" ?>
<ContentPage
    x:Class="XamApp.Views.HelloWorldView"
    xmlns="http://xamarin.com/schemas/2014/forms"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModel="clr-namespace:XamApp.ViewModels"
    Title="Hello World!"
    x:DataType="viewModel:HelloWorldViewModel">
    <StackLayout HorizontalOptions="Center" VerticalOptions="Center">
        <Label Text="{Binding StepsCount, StringFormat='{}Button tapped {0} times!'}" />
        <Button Command="{Binding IncreaseStepsCountCommand}" Text="+" />
    </StackLayout>
</ContentPage>
